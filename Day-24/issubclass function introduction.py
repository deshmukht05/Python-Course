# issubclass() function:
# It returns TRUE if the specified object is a subclass of the specified object, otherwise returns FALSE. 

# Syntax:
# issubclass(object, subclass)

# where object is the name of object and subclass is a class object or tuple of class objects. 